# Cloud deployment (Render) — same as requirements.txt but swaps `dlib`
# (source-only on PyPI, needs CMake + a C++ toolchain that Render's native
# Python runtime doesn't have) for `dlib-bin`, a prebuilt-wheel drop-in
# published by https://github.com/alesanfra/dlib-wheels that installs the
# exact same `dlib` module with no compilation.
#
# `face_recognition` itself still declares a hard dependency on the PyPI
# `dlib` package by name, so it must be installed separately with --no-deps
# (see render.yaml's buildCommand) — otherwise pip ignores dlib-bin and
# tries to build real dlib from source anyway. face_recognition's actual
# import-time requirements (dlib, numpy, Pillow, face_recognition_models)
# are all satisfied by this file.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic[email]>=2.9
pydantic-settings>=2.5
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
supabase==2.7.4
opencv-python-headless>=4.10
numpy>=1.26

# --- Face recognition (prebuilt, no compiler needed) ---
dlib-bin>=20.0.1
face_recognition_models>=0.3.0
setuptools<81  # face_recognition_models still imports the deprecated pkg_resources API

qrcode[pil]==7.4.2
reportlab==4.2.2
openpyxl==3.1.5
pandas>=2.2
python-dotenv==1.0.1
slowapi==0.1.9
loguru==0.7.2
Pillow>=10.4
